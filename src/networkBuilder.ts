export interface NetworkContext {
    clientType: "gquorum" | "jquorum",
    nodeCount: number,
    privacy: boolean,
    outputPath: string
}

export function buildNetwork(context, NetworkContext) {

}